import { useBoardViewContext } from "../../../../context/BoardViewContext";

const BoardWrapper = () => {
  return (
    <div>
      <button>wow</button>
    </div>
  );
};

export default BoardWrapper;
